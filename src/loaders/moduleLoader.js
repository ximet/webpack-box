const { getBabelLoader } = require('./babelLoader.js');
const { imageURLLoader, imageFileLoader } = require('./imageLoader.js');
const { styleLoader, cssLoader } = require('./styleLoader.js');

const getLoaders = (listTypeLoaders) => {
    listTypeLoaders.map((item) => {
        
    })
}

module.exports = {
    getLoaders
}